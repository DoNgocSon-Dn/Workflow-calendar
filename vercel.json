{
  "buildCommand": "npm --prefix frontend ci && node frontend/scripts/set-env.mjs && npm --prefix frontend run build",
  "outputDirectory": "frontend/dist/frontend/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
